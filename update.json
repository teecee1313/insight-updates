{
 "version": "2026.08.12-742-open",
 "url": "https://teecee1313.github.io/insight-updates/",
 "notes": "v742 — Days like today counts EVENTS, not overlapping days: with the new server index each streak of similar days counts once, medians are exact (no more bin rounding), and buckets can name real example days. Rows now read “…events”. Works with the current server too — exact numbers switch on when the new index is pasted."
}